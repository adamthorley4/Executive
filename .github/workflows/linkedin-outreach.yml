name: LinkedIn Outreach Pipeline

# Pipeline archived — LinkedIn outreach removed June 2026
on:
  workflow_dispatch:         # manual trigger only (schedule disabled)

jobs:
  outreach:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: projects/linkedin-outreach

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: projects/linkedin-outreach/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run LinkedIn outreach pipeline
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GOOGLE_SHEETS_SPREADSHEET_ID: ${{ secrets.GOOGLE_SHEETS_SPREADSHEET_ID }}
          GOOGLE_SERVICE_ACCOUNT_EMAIL: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_EMAIL }}
          GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY }}
        run: npm start
